# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  	watch(%r{app/views/.+\.(html)})
    watch(%r{app/assets/.+\.(less|css|js)})
    watch(%r{public/.+\.(css|js|html)})
    watch(%r{conf/messages.+})
end
